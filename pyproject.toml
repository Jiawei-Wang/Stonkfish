[project]
name = "stonkfish"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jiawei Wang", email = "37646262+Jiawei-Wang@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "chess>=1.11.2",
]

[project.scripts]
stonkfish = "stonkfish:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
